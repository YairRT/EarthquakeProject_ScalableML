# Core dependencies
streamlit>=1.52.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=6.0.0
pydeck>=0.9.0

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.2.0

# Hopsworks
# Use specific version that works (4.2.0 works locally, avoid 4.6.0 which has Docker issues)
hopsworks==4.2.0
# Note: Install hsfs with: pip install 'hsfs[python]' (quotes needed for zsh)
hsfs[python]>=3.7.0

# PySpark (required for HSFS Spark engine detection, even if we use Python engine)
pyspark>=3.5.0

# Environment
python-dotenv>=1.0.0
